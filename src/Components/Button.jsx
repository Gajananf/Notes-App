import React from "react";

export const Button = ({e,text,data})=>{
      const handleClick=()=>{
  }
    return(
        <>
        <button onClick={handleClick} className="bg-white w-1/2 text-black px-5 py-2 rounded">
          Submit
        </button >
        </>
    )
}